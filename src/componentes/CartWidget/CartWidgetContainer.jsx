 import { CartWidget } from "./CartWidget"
 import { Link } from "react-router-dom"
 export const CartWidgetContainer = () => {
    return (
        <div>
            <CartWidget/>
        </div>
    )
 }
 